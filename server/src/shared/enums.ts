export enum Role {
  ADMIN = 'admin',
  STUDENT = 'student',
  TEACHER = 'teacher',
}

export enum Difficulty {
  Easy = 'easy',
  Medium = 'medium',
  Hard = 'hard',
}

export enum DurationUnit {
  Day = 'days',
  Week = 'weeks',
  Month = 'months',
  Year = 'years',
}

export enum EnrollmentStatus {
  PENDING = 'pending',
  APPROVED = 'approved',
  REJECTED = 'rejected',
}

export enum RoomStatus {
  OPEN = 'open',
  CLOSED = 'closed',
}
